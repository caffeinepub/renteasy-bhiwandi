# Views Folder

This folder is reserved for server-rendered views (e.g., EJS or Pug templates) if you choose to add them later.

Currently the project is an API-only backend designed to be consumed by the React frontend.
